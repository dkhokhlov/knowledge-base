gdrive-fixture-marker-7f3a2

Deterministic integration-test fixture for the api-gateway /index dir parameter.
Committed under root_tests/gdrive/ (a separate tracked fixture tree, mirrored
from root/). The fixture test indexes this set via /index?dir=gdrive into a
throwaway KB named "gdrive" (name == subdir, the regular flow).