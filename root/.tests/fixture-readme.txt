gdrive-fixture-marker-7f3a2

Deterministic integration-test fixture for the kb-gateway /index path parameter.
Committed under gdrive/.tests/ (a dot-dir) so the full gdrive walk skips it.
The fixture test indexes this set via /index?path=.tests into a throwaway KB.