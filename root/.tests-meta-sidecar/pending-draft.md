# Pending draft fixture

meta-sidecar-marker-7b2f4 is the rare retrieval token. This document is marked
not grounded and pending review in its .meta.json sidecar. The approval status is
pending_review, so it must NOT appear as grounded in the kb skill retrieve join.
