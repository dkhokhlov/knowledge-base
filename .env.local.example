# Copy to .env.local (gitignored). `make bootstrap` copies this file and
# generates BOTH secrets (random) if left empty, then chmods .env.local to 0600.
# You do not normally edit these by hand.

# Open WebUI JWT signing key. Generated by `make bootstrap`. Do not change it
# after users exist, or all sessions are invalidated.
WEBUI_SECRET_KEY=

# Shared bearer token that gates the Graphiti MCP HTTP endpoint (one token for
# all users). Generated by `make bootstrap` and printed to the terminal; copy it
# into your MCP clients as Authorization: Bearer <this value>.
GRAPHITI_API_TOKEN=

# ---- Integration test credentials (used by `make test`).
# Set both to an existing Open WebUI user (e.g. the admin you registered).
# Tests sign in to obtain a JWT, then call the chat REST API. NOT generated by
# `make bootstrap`; fill these in by hand. Leave empty to skip the REST test.
OPENWEBUI_TEST_USER=
OPENWEBUI_TEST_PASSWORD=

# ---- Agent (non-admin) account + API keys. Populated by `make api-keys`
# (run after `make start` and the admin has registered). Leave empty here.
# OPENWEBUI_USER_API_KEY is the read-scoped key to hand to agents; the admin
# key must NOT be given to agents. Set FORCE=1 when running `make api-keys` to
# rotate (replace) the API keys.
OPENWEBUI_USER=
OPENWEBUI_USER_PASSWORD=
OPENWEBUI_ADMIN_API_KEY=
OPENWEBUI_USER_API_KEY=